# Changelog

Notable changes to agent-surface and its first-party MCP services. Format: [Keep a Changelog](https://keepachangelog.com/); the repo is pre-1.0 and unreleased changes land on `main`.

## [Unreleased]

### Added

- **MCP credential env-delivery** - `install` collects each service's declared credentials into a scope-appropriate `.env` (interactive prompts for required keys, headless fails closed; values never enter host config or logs). The file is owner-only by the mechanism the host actually enforces: mode `0600` on POSIX, and on Windows an explicit ACL (inheritance dropped, the owning user granted) because Windows ignores mode bits entirely — if that cannot be applied the install warns, naming the file. Servers that declare credentials launch through a shared env-loader wrapper (materialized into `~/.local/bin` during install with the install-validated Node pinned) that loads the installer-resolved `.env` (ambient env wins) then execs the real command with stdio + working directory preserved; keyless servers keep their direct launch. `.env`/`.env.*` are excluded from Git and the package.
- **Selected-MCP prerequisite provisioning** - `install` detects (read-only) each selected MCP service's executable prerequisites and runtime floors — resolving each binary to an absolute launch path (an existing directory or non-executable file is not accepted as a launch binary) and wiring that absolute path into the generated launch command (including inside the credential wrapper's args), so a tool installed into a directory the host does not have on PATH still launches instead of failing with `spawn <name> ENOENT` — and establishes only what is missing before it wires any host config. Only the services a run will actually wire are provisioned, so a category-filtered install (skills only, say) is never blocked by an MCP prerequisite it does not use. Recipes pick the actual installer for the environment: macOS Homebrew, Linux apt/dnf/pacman/zypper, Windows winget, with the uv standalone installer as a fallback. The executor re-resolves each recipe command against fresh detection before running it, so a prerequisite installed earlier in the same run (e.g. uv into an off-PATH `~/.local/bin`) is invoked by its absolute path — a single `install` bootstraps uv and then uses it, with uv never on the caller's PATH. Chrome DevTools gets a browser on every supported distro: Debian/Ubuntu and Fedora/RHEL install Google Chrome from Google's repo, while Arch and openSUSE install Chromium (Chrome has no official package there); the provisioned browser's absolute path is passed to the server as `--executablePath`, so it drives exactly the browser that was detected rather than relying on channel auto-discovery. A server whose resolved launch binary defers to `#!/usr/bin/env <interp>` (npx, and the MCP bin npx spawns) is launched through the shared wrapper, which puts the install-validated Node at the front of that child's PATH — so the whole npx → MCP → browser chain starts under a host's minimal PATH instead of failing with `env: node: No such file or directory`. The wrapper hands over the credential env-file only to servers that declare credentials, and servers launching a real binary or absolute-interpreter script (synapse, grimoire) keep their direct launch path. OpenOSINT is provisioned through uv with its provider extras (shodan, censys, openai, ollama), which are detected inside OpenOSINT's own interpreter so an already-installed launcher never masks missing extras. Native Windows follows the same rules in its own idiom: executability is decided by PATHEXT (a POSIX-style extensionless path such as `~/.local/bin/uv`, or any non-PATHEXT file, is not a launchable binary and never reads satisfied), a bare command resolves to its `.exe`/`.cmd` through PATHEXT and still matches its registry name, and the wrapper is the install-pinned `node.exe` invoked with the launcher module rather than a shell stub — there is no shebang on Windows, and a `.cmd` wrapper would be unspawnable by the Node-based hosts that make up nearly all of them. npm's `.cmd`/`.bat` console shims (npx, and the recipe commands themselves) are run through `cmd.exe` with per-token quoting, so a `C:\Program Files (x86)\…` path survives intact; `Path` is extended in place rather than shadowed by a second `PATH`. A tool's environment directory is resolved by asking the tool (`uv tool dir`) instead of assuming a layout — uv's differs per platform and is redirectable, so a hard-coded path reported a successful install as missing extras — Windows bootstraps uv through Astral's standalone installer (which targets a declared detect path, keeping the same-run bootstrap closed) rather than a WinGet portable package that lands off the running process's PATH, and an authorized install carries that authorization into WinGet (`--silent --disable-interactivity --accept-package-agreements --accept-source-agreements`) while still declaring that its machine-wide installs need OS elevation. Prerequisites with no runnable recipe on the current platform — e.g. the Synapse/Grimoire repo build on native Windows, which needs a POSIX shell — are reported unavailable rather than run through an unsupported recipe. Interactive installs ask once, `-y` authorizes, headless never prompts, and `--dry-run` only shows the plan. A service whose required prerequisite cannot be established — unauthorized, declined, or unsupportable on this platform — is skipped without wiring while the rest of the install proceeds and the run reports non-zero with guidance; its existing config is preserved and completed prerequisites are reused on re-run. One unavailable MCP server therefore never blocks a user's skills and rules, and only the services a run actually wires are provisioned at all. The MCP-package Node floor (≥22.17) is tracked separately from the root CLI floor (≥20.12).
- **Agent capability packs** - adds optional Chrome DevTools and IDA Pro MCP wiring plus the Archify interactive diagram renderer.
- **Asset categories and cybersecurity expansion** - separates general, optional external, development, cybersecurity, private, and modding installs without adding a profile layer; development workflows, rules, subagents, and tools are no longer emitted by the general baseline, while `hack-skills` remains available through Grimoire and Fenjing/OpenOSINT/pentest-ai remain opt-in MCPs.
- **`dev-core` staged development skill** - turns an independently specified RED core slice into one runnable architectural spine, then pauses before lower-effort feature expansion.
- **Grimoire MCP (v0.1 package; v1.0 tool contract)** - read-only, just-in-time retrieval over the `anthropic-cybersecurity-skills`, `rev-skills`, and `hack-skills` packs from a self-contained `node:sqlite` FTS5 index. Four tools (`grimoire_search`/`list`/`get`/`file_get`) expose source/license attribution and fail closed on stale provenance.
- **First-party MCP auto-wiring across all 22 MCP-capable hosts** - Synapse + Grimoire are generated and non-destructively merged into each host's native config across JSON, TOML, and YAML families. Full matrix: `docs/reference/targets.md`.
- **2026 runtime portfolio refresh** - adds DSH, Qoder, Qwen Code, and Kiro; upgrades Copilot CLI, Grok Build, Antigravity CLI, and Trae; retires VSCodium; records Amp, Auggie, Crush, and Warp as planned.
- **`doctor` MCP health** — checks linked binaries, the synapse sidecar, and grimoire **index freshness** (installed manifest pin vs the repo registry pin).
- **CI** — a Node-22 `mcp` job runs the grimoire (incl. real-pack eval) and synapse package suites + audits on every PR.

### Changed

- **Curated external portfolio** - keeps strong large skill catalogs in Grimoire, retains evaluated references as source-only submodules, and removes thirteen duplicate or unnecessary checkouts. Karpathy guidance is no longer mirrored because canonical workflow/project rules own the behavior; Sanyuan development distribution now keeps only skill authoring and review.
- **Runtime/model routing refresh** - replaces the June model table with current Codex, Claude, DeepSeek, Grok, Cursor, Kimi, and Ollama Cloud recommendations; refreshes headless command shapes and prefers driver-native subagent or agent-manager tools before subprocess orchestration.
- **`ops-clean` simplification gate** - requires a concrete reduction in owners, layers, dependencies, configuration axes, or failure modes instead of counting rearrangement or raw line changes as simplification.
- **`ops-docs` skill** rewritten to the Diátaxis + minimalism model: aggressive, repo-fit, and concise by default.
- **README** rewritten lean (203 → ~100 lines); the full target matrix moved to `docs/reference/targets.md`.
- **Full-access policy ownership** - Kilo and OpenCode full installs replace the complete permission object with wildcard allow while preserving unrelated top-level settings; category-only MCP installs do not alter host-wide permission or sharing controls.
- **Node floor raised to 20.12** - the installer's credential resolution uses the built-in `util.parseEnv`; `engines.node` now declares `>=20.12.0` to match (Node 18 is EOL and was already untested in CI).

### Fixed

- **MCP opt-in** — `--category mcps` without `--service` now selects first-party services only; external MCPs such as `pentest-ai` require an explicit `--service`.
- **Install correctness** — `grimoire-index` wrapper resolves the real entrypoint (derived from `package.json#bin`); a missing required pack fails the install (exit 1) non-destructively instead of silently succeeding.
- **Grimoire provenance and lifecycle** - requires explicit attribution, marks Git-less input `uncommitted`, fingerprints complete skill source, cleans failed publication temporaries, and reopens atomic index replacements.
- **Reinstall safety** - cleans exact declared Gemini/VSCodium and adapter-migration routes while preserving outputs still shared with active targets; maintained JSONC, TOML, and YAML libraries preserve unrelated config; Claude Code and Copilot can share the standard project `.mcp.json` route.
- **Synapse cross-project reads** - binds the cursor to the selected project and returns non-mutable cross-project ids, preventing row-id collisions from reaching local `memory_get` or `memory_forget`.

## Components

- Synapse MCP: `v0.4.0` (shared multi-agent memory + file-lock coordination) — `mcps/synapse/`.
- Grimoire MCP: `v0.1.0` (just-in-time skill retrieval) — `mcps/grimoire/`.
